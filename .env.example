# Database connection string
# SQLite (file-based, no setup needed) - default for development
DATABASE_URL="file:./dev.db"

# For production with PostgreSQL, use:
# DATABASE_URL="postgresql://username:password@localhost:5432/secret_santa?schema=public"

# Admin password (will be hashed when first used)
# Change this to a secure password before deployment
ADMIN_PASSWORD="admin123"
